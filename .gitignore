# colcon / ROS 2 build outputs
build/
install/
log/

# Python bytecode
__pycache__/
*.pyc

# live event store at the workspace root (archived copies are kept under runs/)
/events.db
/events.db-wal
/events.db-shm
